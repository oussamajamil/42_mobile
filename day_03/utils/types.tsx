export type CitiesItem = {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
  country: string;
  admin1: string;
};

export type InformationCity = {
  country: string;
  region: string;
  city: string;
};

export type Positions = {
  latitude: number;
  longitude: number;
};
